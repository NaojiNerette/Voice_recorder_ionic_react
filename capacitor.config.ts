import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.recetasmedicas',
  appName: 'RecetasMedicas',
  webDir: 'dist'
};

export default config;
